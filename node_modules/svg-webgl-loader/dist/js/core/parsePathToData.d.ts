import { ViewBox } from '@/types/types';
export declare function parsePathToData(paths: any): {
    data: any[];
    boundary: {
        minX: number;
        minY: number;
        maxX: number;
        maxY: number;
    };
};
interface IBoundary {
    minX: number;
    maxX: number;
    minY: number;
    maxY: number;
}
export declare function getViewBox(viewBox: ViewBox, boundary: IBoundary, needTrim: boolean): ViewBox;
export {};
